// Phase 0 placeholder — replaced with the real boot() in Phase 1.
const root = document.getElementById('root');
if (root) {
  root.textContent = 'WeAgri (Node.js + TypeScript scaffold) — Phase 0 OK';
}
